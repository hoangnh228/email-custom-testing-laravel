## Laravel tracking email testing

The sample test with mail tracking in Laravel follow on Laracasts tutorials.

## Run test

- Run command: `composer install` to install packages.
- Use command: `vendor/bin/phpunit` to running test